<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.davadose">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CharacterSelection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true" />

        <activity android:name=".DoctorSignup" />
        <activity android:name=".PatientSignupOne" />
        <activity android:name=".PatientSignupTwo" />
        <activity android:name=".PatientSignupThree" />
        <activity android:name=".DoctorHome" />
        <activity android:name=".PatientHome" />
        <activity android:name=".NewPrescriptionActivity" />
        <activity android:name=".PatientDetails" />
        <activity android:name=".NewPrescriptionTwo" />
        <activity android:name=".ConsultedDoctors" />
        <activity android:name=".MyPrescription" />
        <activity android:name=".MyDosage" />
    </application>

</manifest>